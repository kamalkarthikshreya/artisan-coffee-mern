{
    "name": "artisan-coffee-mern-root",
    "version": "1.0.0",
    "scripts": {
        "start": "node server/server.js",
        "postinstall": "cd server && npm install",
        "server": "cd server && npm start",
        "client": "cd client && npm run dev",
        "install-all": "cd server && npm install && cd ../client && npm install"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}